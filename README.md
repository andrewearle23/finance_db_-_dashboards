# finance_db_-_dashboards
A lightweight ETL pipeline that extracts data from SAP Business One (SQL Server), transforms it, and loads it into a MySQL reporting database. Power BI dashboards connect to the MySQL layer for reporting and dashboards
